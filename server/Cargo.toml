[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
serde = { workspace = true }
bincode = { workspace = true }
tokio = { version = "1", features = ["full"] }
