[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
bincode = { workspace = true }
bevy = { workspace = true, optional = true }

[features]
bevy_support = ["dep:bevy"]
